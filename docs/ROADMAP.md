# ROADMAP.md — نظام إدارة الإجازات والمستندات (Holidays)

> **حالة الوثيقة:** آخر ملف في سلسلة التوثيق (10 أيلول 2026) — يعتمد على كل الملفات السابقة ولا يكرر تفاصيلها. مُنظَّم بأفق زمني تقريبي (قريب/متوسط/بعيد) **وليس بتواريخ تقويمية محدَّدة**، لأن لا مواعيد رسمية مؤكَّدة من مالك المشروع بعد لأي بند هنا. عند تأكيد أي موعد فعلي، يُضاف صراحة في نفس البند بدل ترك الغموض.

---

## كيف تُقرأ هذه الخريطة

كل بند أدناه يشير لمصدره في التوثيق السابق (لا تكرار) ويوضّح **هل هو جاهز للتنفيذ الآن، أم ينتظر بوابة قرار (Decision Gate) لم تُفتَح بعد.** القسم §5 يجمع كل بوابات القرار في مكان واحد لأنها الأكثر أهمية للمتابعة.

---

## 1. الأفق القريب (Near-Term — التالي مباشرة، لا ينتظر أي قرار معلَّق)

هذه البنود **جاهزة للتنفيذ فوراً** — تم إنجاز عدد منها بنجاح، والباقي جاهز:

| البند | المصدر | الحالة / الملاحظة |
|---|---|---|
| تنفيذ فعلي لـ `npm test` كاملاً (`leaveService.test.js`) على بيئة التطوير | `TECHNICAL-SPECIFICATION.md` §5 | ✅ **مُنجَز بالكامل (15 أيلول 2026):** نجاح 200/200 اختبار عبر 28 جناح فحص ومطابقة 50/50 قناة IPC بنسبة 100% |
| حصر معمارية البناء لـ Windows على `x64` فقط | `DECISIONS.md` ADR-018 | ✅ **مُنجَز بالكامل (15 أيلول 2026):** إسقاط `ia32` وحصر NSIS على `x64` تمهيداً لـ Microsoft Store |
| موديول الأقسام وتصدير الإجازات المنتهية ومحرك التداخل | `DECISIONS.md` ADR-020–023 | ✅ **مُنجَز بالكامل (15 أيلول 2026):** ميزات حيوية مكتملة ومختبرة ومرفوعة |
| معالجة ثغرة `adm-zip` (رفض الروابط الرمزية والـ Zip Slip) | `IMPLEMENTATION-PLAN.md` البند A | ✅ **مُنجَز بالكامل (17 أيلول 2026):** حماية كاملة عبر `pathValidator.js` واجتياز 10/10 اختبارات |
| اختبار أداء فعلي بحجم 5000 موظف | `IMPLEMENTATION-PLAN.md` البند B | ✅ **مُنجَز بالكامل (17 أيلول 2026):** استجابة فورية (<5ms للبحث والتقارير، 36ms للإدخال، 11.28MB ذاكرة) موثقة بـ `PERFORMANCE-BENCHMARK-RESULTS.md` |
| شاشة إدارة أنواع الإجازات ودعم مكان الإجازة | `PRODUCT-SPECIFICATION.md` §3.4 | ✅ **مُنجَز بالكامل (18 أيلول 2026):** إدارة مركزية لأنواع الإجازات مع حماية الأنواع الأساسية وتوثيق مكان الإجازة واجتياز 14 حزمة اختبار بنسبة 100% |
| شبكة أمان العملية وحماية انهيار الواجهة وتوقيت بغداد | `DECISIONS.md` ADR-031 | ✅ **مُنجَز بالكامل (18 أيلول 2026):** معالجات `uncaughtException` / `unhandledRejection` وإغلاق قاعدة البيانات الآمن وحماية `render-process-gone` وعزل كتل الإقلاع واحتساب الإجازات بتوقيت بغداد واجتياز 15 حزمة اختبار بنسبة 100% |

**معيار الانتقال للأفق التالي:** ✅ **تحقق بالكامل** بإغلاق اختبار الأداء بحجم 5000 موظف بنتائج فعلية موثَّقة تثبت جاهزية محرك SQLite للحد الأقصى المخطط له.

---

## 2. الأفق المتوسط (Medium-Term — يعتمد على إغلاق الأفق القريب)

### 2.1 بدء التوسّع لقسم/دائرة إضافية (أول توسّع فعلي)

**المصدر:** `PRD.md` §3 — قرار استراتيجي مؤكَّد مسبقاً (ليس بوابة قرار مفتوحة، بل تنفيذ لقرار متَّخذ فعلاً).

**ما يتطلبه التنفيذ (لا تعديل معماري، مجرد تكرار نشر):**
- نسخة تثبيت مستقلة جديدة (نفس المثبِّت الحالي `x64`) على جهاز القسم الجديد.
- قاعدة بيانات SQLite جديدة فارغة (بدون أي مشاركة أو مزامنة مع نسخة القسم الأول — بالتصميم).
- **لا حاجة لانتظار أي بوابة قرار في §5** — هذا التوسّع لا علاقة له بقرار Windows 7/Store.

**نقطة انتباه من `IMPLEMENTATION-PLAN.md` الجزء الثالث:** إن ظهرت لاحقاً حاجة فعلية لتقرير موحَّد يقارن بيانات عدة أقسام مركزياً، هذا **يفتح بوابة قرار معمارية جديدة تماماً** غير موجودة حالياً في `DECISIONS.md` — لا يُفتَرض حلّها مسبقاً هنا.

### 2.2 تحسينات الجودة غير العاجلة

**المصدر:** `IMPLEMENTATION-PLAN.md` الجزء الثاني/D — دفعة واحدة مناسبة بعد استقرار أول توسّع فعلي (§2.1)، لتجنّب تشتيت الانتباه عن مراقبة التوسّع الأول: إضافة `.eslintrc`، حقل `engines` في `package.json`، حذف الكود الميت (`RequiresOrderRef`/`trg_enforce_order_ref`) بعد تأكيد نهائي أنه تعطيل دائم لا مؤقت، وفصل منطق النسخ الاحتياطي إلى `BackupService.js` مستقل.

---

## 3. الأفق البعيد (Long-Term — خلف بوابات قرار غير مفتوحة بعد)

كل بند هنا **لا يبدأ تخطيطه التفصيلي قبل فتح بوابة القرار المرتبطة به في §5** — إدراجه هنا للوعي المسبق فقط، لا كالتزام تنفيذ.

| البند | المصدر | البوابة المطلوبة |
|---|---|---|
| النشر على Microsoft Store (يتطلب ترقية Electron والتخلي عن Windows 7/8.1) | `DECISIONS.md` ADR-013 | القرار الاستراتيجي الكامل — لم يُتَّخذ بعد |
| نظام صلاحيات متعدد الأدوار (Role-Based Access Control) | `PRD.md` §5 | حاجة فعلية تنشأ من تعدد المستخدمين — `AuditLogs.UserID` مُجهَّز مسبقاً لكن غير مفعَّل |
| تقرير مركزي موحَّد عبر عدة أقسام | `IMPLEMENTATION-PLAN.md` الجزء الثالث | لم تُطرَح كحاجة فعلية بعد — سؤال مفتوح فقط |
| أي تكامل مع نظام رواتب أو حضور/انصراف | `PRD.md` §5 (خارج النطاق صراحة) | لا يُدرَج حتى على هذه الخريطة كاحتمال قريب — قرار نطاق أساسي مختلف كلياً، وليس امتداداً طبيعياً لما هو قائم |

---

## 4. مؤشرات النجاح المرتبطة (رابط مباشر لـ `PRD.md` §4)

خريطة الطريق هذه لا تقاس بعدد الميزات المُنجَزة، بل بنفس معايير `PRD.md`:

- **صفر أخطاء في حساب الأرصدة** → يُراقَب باستمرار عبر سجل التدقيق (`AuditLogs`) وأي بلاغ فعلي من مستخدم النظام، لا عبر افتراض أن الاختبارات الآلية كافية وحدها.
- **إلغاء الاعتماد الكامل على الورق** → مؤشر أوضح بعد اكتمال §2.1 (أول توسّع فعلي) — إن استمر أي قسم بالاعتماد الموازي على الورق بعد التوسّع، هذا إشارة على مشكلة تبنّي (Adoption) لا مشكلة تقنية، تستحق فتحها كبند منفصل.
- **تسريع وقت إصدار التقارير** → يحتاج قياساً فعلياً "قبل/بعد" لم يُوثَّق رقمياً بعد (نقطة مفتوحة أشير إليها أصلاً في `PRD.md` §4).

---

## 5. بوابات القرار المفتوحة (Decision Gates) — القائمة الموحَّدة

هذا القسم يجمع **كل** القرارات المعلَّقة عبر كامل سلسلة التوثيق في مكان واحد، لأنها الأكثر أهمية لمتابعتها دورياً:

| # | القرار | أين وُثِّق بالتفصيل | يفتح الباب أمام |
|---|---|---|---|
| 1 | Windows 7/8.1 مقابل Microsoft Store | `DECISIONS.md` ADR-013 | كل بنود §3 الأولى في هذا الملف |
| 2 | حاجة فعلية لنظام صلاحيات متعدد الأدوار | `PRD.md` §5 | بند §3 الثاني |
| 3 | حاجة فعلية لتقرير مركزي عبر عدة أقسام | `PRD.md` §3، `IMPLEMENTATION-PLAN.md` الجزء الثالث | بند §3 الثالث |

**لا تُفتَح أي بوابة من هذه دون قرار صريح من مالك المشروع موثَّق كـ`ADR` جديد في `DECISIONS.md` أولاً** — هذا الترتيب (قرار موثَّق ← ثم تخطيط تقني) هو نفسه الذي أثبت نجاحه فعلياً في دورة N1–N4 المُنجَزة (`CHECKPOINT.md` الجلسة #1)، ولا يوجد سبب لتغييره.

---

## ملخص السلسلة الكاملة (للمرجعية السريعة)

هذه الخريطة تُغلِق سلسلة التوثيق الكاملة التي بُنيت بالترتيب التالي، من "الواقع كما هو" إلى "ماذا بعد":

```
ARCHITECTURE.md  →  DATA-MODEL.md  →  DECISIONS.md  →  TECHNICAL-SPECIFICATION.md
       →  PRODUCT-SPECIFICATION.md  →  PRD.md  →  IMPLEMENTATION-PLAN.md
              →  CHECKPOINT.md  →  ROADMAP.md (هذا الملف)
```

كل ملف يبقى **حياً** (Living Document) يُحدَّث عند أي تغيير فعلي، لا يُعاد بناؤه من الصفر. نقطة البداية الصحيحة لأي عمل مستقبلي هي دائماً: `CHECKPOINT.md` (أين توقّفنا؟) → هذا الملف (ما التالي منطقياً؟) → `IMPLEMENTATION-PLAN.md` (كيف نُنفِّذه بأمان؟).

---

*آخر تحديث: 18 أيلول 2026 — إغلاق وتحديث سلسلة التوثيق الكاملة حتى ADR-031. مراجع تكميلية: كل الملفات المذكورة أعلاه.*
